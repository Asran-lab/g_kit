name: g_common
description: "A new Flutter package project."
version: 0.0.1
homepage: ''
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Archive for ZIP compression
  archive: ^4.0.7
  
  # HTTP for web downloads (already imported in foundation)
  http: ^1.1.0

  g_lib:
    git:
      url: git@github.com:Asran-lab/g_kit.git
      path: g_lib
      ref: main

  g_model:
    git:
      url: git@github.com:Asran-lab/g_kit.git
      path: g_model
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:

name: g_model
description: "A new Flutter package project."
version: 0.0.1
homepage: 'https://github.com/g-kit/g_kit'
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  freezed_annotation: ^3.1.0
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  freezed: ^3.2.0
  build_runner: ^2.6.0
  json_serializable: ^6.10.0

flutter:

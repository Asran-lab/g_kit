name: g_lib
description: "A new Flutter package project."
version: 0.0.1
homepage: ''
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  # 유틸성 라이브러리
  path_provider: ^2.1.5          # 파일 경로 관련
  app_links: ^6.4.0              # 앱 링크 관련
  crypto: ^3.0.6                 # 해시 관련
  device_info_plus: ^11.5.0      # 기기 정보 관련
  webview_flutter: ^4.13.0       # 웹뷰 관련
  url_launcher: ^6.3.2           # 링크 관련
  

  # 인증 라이브러리
  local_auth: ^2.3.0             # 생체 인식 관련

  # 권한 라이브러리
  permission_handler: ^12.0.1     # 권한 관리

  # 공유 기능 라이브러리
  share_plus: ^11.0.0            # 공유 기능 관련

  # 스토리지 라이브러리
  sqflite: ^2.4.2               # 로컬 db 데이터베이스
  flutter_secure_storage: ^9.2.4 # 보안 스토리지
  shared_preferences: ^2.5.3     # 공유 환경 변수

  # 네트워크 라이브러리
  dio: ^5.8.0+1                 # 네트워크 라이브러리
  socket_io_client: ^3.1.2      # 소켓 라이브러리
  http_certificate_pinning: ^3.0.1 # 인증서 파이닝 관련

  # 라우팅 라이브러리
  go_router: ^16.0.0             # 라우팅 라이브러리

  # 이미지 라이브러리
  image: ^4.5.4                   # 이미지 디코딩 관련
  flutter_image_compress: ^2.4.0  # 이미지 압축 관련
  lottie: ^3.3.1                  # 애니메이션 라이브러리
  image_picker: ^1.1.2            # 이미지 선택 관련
  image_cropper: ^9.1.0           # 이미지 크롭 관련

  # STT 라이브러리
  speech_to_text: ^7.3.0          # STT 라이브러리

  # WorkManager 라이브러리
  # workmanager: ^0.9.0+3           # 백그라운드 업데이트 관련

  # Home Widget 라이브러리
  home_widget: ^0.8.0             # 홈 위젯 관련

  # 네비게이션 라이브러
  animated_bottom_navigation_bar: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
